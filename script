node{
    try{
        echo "shabbier"
    }
    currentBuild.result = 'SUCCESS'
}
catchError{
    currentBuild.result = 'FAILURE'
}
finally {
    mail to: 'shabbir0473@gmail.com',
    subject: 'build status',
    body: "${env.BUILD_URL} has result ${currentBuild.result}",
}

